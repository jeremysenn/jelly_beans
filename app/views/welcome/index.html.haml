.jumbotron.jumbotron-fluid.text-xs-center{style: 'background-color: white;'}
  - unless logged_in?
    %h1.display-3 
      %i.fa.fa-plus-square.text-primary
      Jelly Bean
    %p.lead A bean-shaped candy with a jellylike center and a firm sugar coating.
  - else
    %h1.display-3
      = current_company.name
      - unless current_company.customers.blank?
        %br
        = form_for Transaction.new do |form|
          = form.hidden_field :user_id, value: current_user.id
          = form.hidden_field :category, value: 'Sale'
          = form.hidden_field :status, value: 'Incomplete'
          = form.hidden_field :customer_id, value: current_company.customers.order('first_name ASC').first.id
          %button.btn.btn-primary.btn-lg{:type => "submit"} New Transaction
    -#
      %p.lead A bean-shaped candy with a jellylike center and a firm sugar coating.